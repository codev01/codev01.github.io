{
	"private": true,
  "scripts": {
    "start": "run-p browser-sync sass:watch",
    "build": "run-s sass:compile",
    "sass:compile": "sass --no-source-map sass/:css/ --style compressed",
    "sass:watch": "sass --no-source-map sass/:css/ --watch",
		"browser-sync": "browser-sync start --config .browser-sync.js"
  },
  "devDependencies": {
    "@rx1310/kalium19": "^3.0.4",
    "browser-sync": "^2.27.9",
    "npm-run-all": "^4.1.5",
    "sass": "^1.50.0"
  },
  "dependencies": {
    "@rx1310/grid-v4": "^4.4.2",
    "sanitize.css": "^13.0.0"
  }
}
